---
"@jmfederico/pi-web": patch
---

Add `context.navigate(destination)` for browser plugin actions and workspace panels, using normal host navigation and unavailable-destination UI. Captain's Log translations now offer an Open source session action.
